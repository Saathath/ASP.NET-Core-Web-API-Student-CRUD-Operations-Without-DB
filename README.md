## How to Run the Project

1. Install .NET SDK (7/8)
2. Clone or download the repository
3. Open terminal in project folder
4. Run:
   dotnet restore
   dotnet build
   dotnet run
5. Open browser:
   https://localhost:7004/swagger

This project does not require any database (in-memory CRUD).
